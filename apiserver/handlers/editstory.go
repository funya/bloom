package handlers

import (
	"net/http"
)

// EditStoryhandler grabs the user name and creates
// an empty story timeline for them under their name, so that
// they could edit and enter events in the story timeline
func EditStoryhandler(w http.ResponseWriter, r *http.Request) {

}
